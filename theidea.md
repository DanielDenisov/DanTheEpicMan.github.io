Website:
Entire website it terminal themed, not like hacker in a movie, like actually same phont and type of grey as in a terminal. 

The entire website has basically like 3 main colors, the background, background text and foreground text.

Some like precedural generation might be in order.

Basically, it kinda looks like: https://www.contentarchitecture.dev/?ref=landing.love specifically the like "art" on the page, except maybe built a bit more simpely.

So basically this is going to be a website for my self/ a portfolio website.

The entire website is build from the background color, and then on top, a faint overlay of a bunch of words (from somehting), there is them some other things, one being a sort of "visibility layer", this will basically be like a bitmask type thing, were it creates an image (ie the cpp logo or python or whatever) by highlighting character (although since this is a bitmask, its like more of a gradient, like 1/2 of a character can be lit up, or a gradient accross a chatacter, not just a binary yes/no).

Then either as a layer on top, or, would be a lot cooler as like part of the embeded text (although a lot more difficult) would just be the companiying text going along with the image. 

Then at the top, like a nav bar thats like super simple:
+-------+--------+-------+
| text  | text 2 | text 3|
+-------+--------+-------+